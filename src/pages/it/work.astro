---
import SectionPage from '../../components/pages/SectionPage.astro';
import { getPage } from '../../lib/pages';

const page = await getPage('it', 'work');
---

<SectionPage locale="it" page={page} />
